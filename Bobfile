version: 1

@as = aasdasd

hello() {
    print("hello world")
}

helloUser(user!) {
    workdir(.build)
    print(hello ${user})
    git status
}

size(out=.build/dev) {
    du -sh ${out}
}

@tes = wow
bd(out=.build/dev) {
    // @tes = kill me
    //clear
    go build -o ${out}
    print(${tes})

    //./${out}
    // ./dev hello2 user=lmao
}

install(){
    go install
}
