version: 1

as := aasdasd

hello() {
    print("hello world")
}

helloUser(user: str!, opt lol) {
    workdir(.build)
    print(hello ${user})
    git status
}

bd(out=.build/dev) {
    //clear
    go build -o ${out}
    ./${out}
    // ./dev hello2 user=lmao
}
